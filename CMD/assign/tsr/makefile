#
# A Makefile for ATTRIB
# (c) osFree project,
# author, date
#

PROJ = asgn_tsr
TRGT = $(PROJ).com
DESC = Assign TSR part
srcfiles = $(p)asgn_tsr$(e)
COM = 1
NOLIBS = 1

!include $(%ROOT)tools/mk/appsdos.mk
!include $(MYDIR)..$(SEP)port$(SEP)assign.mk

.asm: $(PORT_BASE)source$(SEP)assign


